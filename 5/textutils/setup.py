"""Setup configuration for textutils package."""

from setuptools import setup, find_packages

setup(
    name='textutils',
    version='0.1.0',
    description='A text processing utilities package',
    author='Python Seminar',
    packages=find_packages(),
    python_requires='>=3.8',
    install_requires=[],
)

